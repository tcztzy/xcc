# Roadmap

- Eliminate clang fallbacks so `--backend=xcc` compiles CPython alone.
- Add Linux/ELF support via the same libLLVM-C ctypes path.
- Support C11 features still missing: variadic functions, bitfields, `_Complex`.
