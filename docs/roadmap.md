# Roadmap

- Shrink Clang-suite skips with one tested behavior slice at a time.
- Expand native macOS `arm64` backend coverage.
- Add native object emission.
- Add Linux/ELF support with glibc and musl validation.
- Reintroduce CPython translation-unit trials when compiler coverage is ready.
